<div class="container mx-auto px-4 py-8">
  <div class="mb-8 flex items-center">
      <ng-icon name="heroChevronLeft"></ng-icon>
      <a href="/admin/reservation" class="cursor-pointer">Back</a>
  </div>
  <h1 class="text-2xl font-bold text-gray-800 mb-6">
    {{ action }}
  </h1>
  <form class="space-y-6" [formGroup]="reservationForm" (ngSubmit)="onSubmit()">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div class="space-y-4">
        <div>
          <label for="customerId" class="block text-sm font-medium text-gray-700">Customer</label>
          <input formControlId="customerId" type="text" id="customerId" name="customerId" placeholder="Enter customer name" [readonly]="action === 'Reservation Details' ? true : null" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>
        <div>
          <label for="customerName" class="block text-sm font-medium text-gray-700">Customer Name</label>
          <input formControlName="customerName" type="text" id="customerName" name="customerName" placeholder="Enter customer name" [readonly]="action === 'Reservation Details' ? true : null" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>

        <div>
          <label for="checkInDate" class="block text-sm font-medium text-gray-700">Check-in Date</label>
          <input formControlName="checkInDate" type="date" id="checkInDate" name="checkInDate" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>
        <div>
          <label for="checkOutDate" class="block text-sm font-medium text-gray-700">Check-out Date</label>
          <input formControlName="checkOutDate" type="date" id="checkOutDate" name="checkOutDate" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>

      </div>
      <div class="space-y-4">
        <div>
          <label for="phone" class="block text-sm font-medium text-gray-700">Phone</label>
          <input formControlName="phone" type="text" id="phone" name="phone" placeholder="Enter phone" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>

        <div>
          <label for="price" class="block text-sm font-medium text-gray-700">Price</label>
          <input formControlName="price" type="text" id="price" name="price" placeholder="Price" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
        </div>

        <div>
          <label for="status" class="block text-sm font-medium text-gray-700">Reservation Status</label>
          <select
            id="status"
            name="status"
            formControlName="status"
            [disabled]="action === 'Reservation Details'"
            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
          >
          </select>
        </div>
      </div>
    </div>
    <div class="flex justify-center space-x-3">
      <button *ngIf="action !== 'Reservation Details'" (click)="onClose()" class="px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2">Cancel</button>
      <button *ngIf="action !== 'Reservation Details'" type="submit" class="px-4 py-2 bg-gray-800 text-white rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2">Save</button>
    </div>
  </form>
</div>


<ng-container *ngIf="selectedReservation">
    <div class="fixed inset-0 flex items-center justify-center z-50">
      <div class="modal-background absolute inset-0 bg-black opacity-50" (click)="closeDetailsModal()"></div>
      <div class="modal-content relative bg-white rounded-lg shadow-lg p-6 z-10">
        <h2 class="text-xl font-bold mb-4">Reservation Details</h2>
  
        <!-- Close button -->
        <button (click)="closeDetailsModal()"
                class="absolute top-4 right-4 text-gray-500 hover:text-gray-700 z-20">
          &times;
        </button>
  
        <div class="space-y-2">
          <p><strong>Reservation ID:</strong> {{ selectedReservation.id }}</p>
          <p><strong>Created By:</strong> {{ selectedReservation.createdBy }}</p>
          <p><strong>Created At:</strong> {{ selectedReservation.createdTime | date }}</p>
          <p><strong>Updated By:</strong> {{ selectedReservation.updatedBy }}</p>
          <p><strong>Updated At:</strong> {{ selectedReservation.updatedTime | date }}</p>
        </div>
      </div>
    </div>
  </ng-container>
